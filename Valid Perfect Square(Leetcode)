Given a positive integer num, return true if num is a perfect square or false otherwise.

#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int num;
    cin >> num;

    int root = sqrt(num);
    if(root * root == num)
        cout << "true";
    else
        cout << "false";

    return 0;
}
